# Supabase Configuration for Streamlit
# This is a template file. Copy this to secrets.toml and fill in your actual values.
# secrets.toml should NOT be committed to git (it's in .gitignore)

supabase_url = "YOUR_SUPABASE_PROJECT_URL"
supabase_key = "YOUR_SUPABASE_ANON_KEY"
